//store information from user's username and its token.
export interface User {
     gender: string;
     userName: string;
     token: string;
     photoUrl: string;
     knownAs: string;
     Gender: string;
}